#include <bits/stdc++.h>

using namespace std;

inline void io_setup(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
}

vctor<vector<int>> table(1000,vector<int>());

int main(){
    io_setup();
    int g,n,m;
    cin >> g >> n >> m;
    for(int i=0;i<n;i++){
        int a,k;
        cin >> a >> k;
        
    }
}